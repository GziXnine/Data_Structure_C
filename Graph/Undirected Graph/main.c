#include "graph_ds.h"

Graph graph;
uint32_t numVerticesResult = ZERO_INIT, numEdgesResult = ZERO_INIT;
uint32_t numVertices = ZERO_INIT, defaultData = ZERO_INIT;
return_status_t status = GRAPH_NOK;

int main()
{

  return 0;
}