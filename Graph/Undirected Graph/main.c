#include "graph_ds.h"

graph_ds_t graph;
uint32_t numVertices = ZERO_INIT, numEdges = ZERO_INIT;
uint32_t degree = ZERO_INIT, numAdjacent = ZERO_INIT;
return_status_t status = GRAPH_NOK;

int main()
{

  return 0;
}