#include "binarySearchTree.h"

tree myTree;
uint32_t size = ZERO_INIT, depth = ZERO_INIT;
uint32_t checkElement = ZERO_INIT, deletedValue = ZERO_INIT;
return_status_t retVal = BINARY_TREE_NOK; 

// Function prototype
void printNode(uint32_t data);

int main()
{
 
  return 0;
}

void printNode(uint32_t data)
{
  printf("%u ", data);
}